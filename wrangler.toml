name = "recetas-app"
type = "javascript"

# Cloudflare Pages configuration
[env.production]
name = "recetas-app-prod"

[build]
command = ""
cwd = "./frontend"

[site]
bucket = "./frontend"

# API endpoint for development (local Node.js backend)
# For production: use Cloudflare Workers if backend needed
