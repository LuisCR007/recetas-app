# 📊 DIAGRAMA VISUAL: Git + Docker + Cloudflare

## ARQUITECTURA GENERAL

```
┌──────────────────────────────────────────────────────────────────┐
│                          TU COMPUTADORA                          │
│                   (Desarrollo Local)                             │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────────────────────┐                         │
│  │  Frontend (HTML/CSS/JS)            │                         │
│  │  ├── index.html                    │                         │
│  │  ├── receta.html                   │                         │
│  │  ├── css/styles.css                │                         │
│  │  └── js/app.js                     │                         │
│  └────────────────────────────────────┘                         │
│                                                                  │
│  ┌────────────────────────────────────┐                         │
│  │  Backend (Node.js + Express)       │                         │
│  │  ├── server.js                     │◄──┐                     │
│  │  ├── database.js                   │   │                     │
│  │  └── datos.js                      │   │                     │
│  └────────────────────────────────────┘   │                     │
│         │                                  │                     │
│         ▼                                  │                     │
│  ┌────────────────────────────────────┐   │                     │
│  │  SQLite Database                   │   │                     │
│  │  └── recetas.db                    │   │                     │
│  └────────────────────────────────────┘   │                     │
│                                            │                     │
│                                    localhost:3000                │
└──────────────────────────────────────────────────────────────────┘
                            │
                  npm start / npm run dev
                            │
            ┌───────────────┴───────────────┐
            │                               │
            ▼                               ▼
   ┌──────────────────┐          ┌──────────────────┐
   │   GIT COMMIT     │          │   DOCKER BUILD   │
   │                  │          │                  │
   │ git add .        │          │ docker build -t  │
   │ git commit -m    │          │ recetas-app:1.0  │
   │ git push         │          │                  │
   └────────┬─────────┘          └────────┬─────────┘
            │                             │
            ▼                             ▼
   ┌─────────────────────────────────────────────┐
   │            GITHUB REPOSITORY                │
   │  https://github.com/usuario/recetas-app    │
   │                                             │
   │  ├── frontend/                              │
   │  ├── backend/                               │
   │  ├── Dockerfile                    ◄────────┼── docker-compose.yml
   │  ├── docker-compose.yml            ◄────────┼── .github/workflows/
   │  ├── .github/workflows/deploy.yml  ◄────────┴── deploy.yml (CI/CD)
   │  └── ...                                    │
   └────────┬─────────────────────────────────────┘
            │ (Webhook automático al push)
            │ GitHub Actions ejecuta
            ▼
   ┌─────────────────────────────────────────────┐
   │        CLOUDFLARE PAGES (CI/CD)             │
   │                                             │
   │  1️⃣  Detecta cambios en main branch         │
   │  2️⃣  npm run build                          │
   │  3️⃣  Ejecuta tests (opcional)               │
   │  4️⃣  Despliega en CDN Global                │
   │  5️⃣  Sube a 200+ servidores worldwide       │
   │                                             │
   └────────┬─────────────────────────────────────┘
            │
            ▼
   ┌─────────────────────────────────────────────┐
   │    SITIO EN PRODUCCIÓN - EN VIVO 🌐         │
   │                                             │
   │  https://recetas-app.pages.dev              │
   │                                             │
   │  Disponible en todo el mundo, 24/7          │
   │  Con SSL, CDN, y máxima velocidad           │
   │                                             │
   └─────────────────────────────────────────────┘
```

---

## FLUJO DE TRABAJO

### Hoy (Desarrollo Local)
```
┌─────────────┐
│ Editar Code │
│  en VS Code │
└──────┬──────┘
       │
       ▼
┌──────────────┐        ┌─────────────────┐
│ npm run dev  │ ◄──►  │ localhost:3000   │
│              │        │ (Ver cambios)    │
└──────┬───────┘        └─────────────────┘
       │
       ▼
┌─────────────────┐
│  Funciona?      │
│  ✓ Sí ──► Commit
│  ✗ No ──► Debug
└────────┬────────┘
         │
         ▼
┌──────────────────────┐
│  git add .           │
│  git commit -m "..." │
│  git push            │
└──────────┬───────────┘
           │
           ▼ (Automático)
┌──────────────────────────────────┐
│  Cloudflare Pages (Producción)   │
│  Build → Deploy → En vivo         │
└──────────────────────────────────┘
```

---

## ESTADIOS DE LA APLICACIÓN

### 🟢 LOCAL (Tu Computadora)
```
npm run dev
    ↓
localhost:3000
    ↓
Solo tú ves cambios
```

### 🔵 STAGING (GitHub)
```
git push
    ↓
GitHub (Repositorio)
    ↓
Control de versiones
    ↓
Otros pueden clonar
```

### 🔴 PRODUCCIÓN (Cloudflare)
```
GitHub Actions
    ↓
Build automático
    ↓
Deploy automático
    ↓
recetas-app.pages.dev
    ↓
Mundo entero puede verlo
```

---

## MAPEO GIT

```
┌───────────┐
│  Local    │
│ (Tu PC)   │
└─────┬─────┘
      │ git push
      │
      ▼
┌──────────────┐        ┌──────────────┐
│   main       │        │  desarrollo  │  (Ramas opcionales)
│  (Origin)    │        │   (Origin)   │
└──────┬───────┘        └──────┬───────┘
       │                       │
       │ (Protected branch)    │
       │ (Solo merges)         │
       │                       │
       ▼                       ▼
  GitHub (Remoto)
  
  master/main = Código en producción
  develop = Siguiente release
  feature/* = Nuevas características
```

---

## DOCKER CONTAINERS

```
┌─────────────────────────────────────────┐
│         docker-compose up -d            │
└──────────────┬──────────────────────────┘
               │
        ┌──────┴──────┐
        │             │
        ▼             ▼
   ┌──────────┐  ┌──────────┐
   │ Backend  │  │ Frontend │
   │ Container│  │Container │
   │          │  │          │
   │ Port     │  │ Port     │
   │ 3000:3000│  │ 3001:3001│
   │          │  │          │
   │NodeJS    │  │  NodeJS  │
   │Express   │  │  (Dev)   │
   │SQLite    │  │          │
   └──────────┘  └──────────┘
        │             │
        └──────┬──────┘
               │
        localhost network
        
   Visualizar:
   docker ps          (Ver contenedores activos)
   docker logs -f     (Ver logs)
   docker exec -it    (Entrar al contenedor)
```

---

## GIT WORKFLOW: Rama Protegida

```
                    ┌─── feature/nueva-funcionalidad
                    │
                    ▼
               Tu rama local
                    │
               (Haces cambios)
                    │
              git commit -m "..."
                    │
              git push origin
                    │
                    ▼
               Pull Request
              (Revisión de código)
                    │
              (Merge al main)
                    │
                    ▼
               ┌─────────────────────┐
               │  main (Protected)    │ ◄── Cloudflare automático
               │  ├─ commits         │        |
               │  │  ├─ CI/CD        │        ├─ Build
               │  │  ├─ Tests        │        ├─ Deploy
               │  │  └─ deploy.yml   │        └─ En Vivo
               │  └─ production      │
               └─────────────────────┘
```

---

## RESUMEN RÁPIDO

| Herramienta | Función | Comando |
|---|---|---|
| **GIT** | Control de versiones | `git push` → sube a GitHub |
| **GitHub** | Repositorio remoto | Almacena tu código |
| **GitHub Actions** | CI/CD automático | Ejecuta build al hacer push |
| **Docker** | Containerización | `docker build` & `docker run` |
| **Docker Compose** | Orquestar contenedores | `docker-compose up` |
| **Cloudflare** | Hosting + CDN + Deploy | Aloja tu sitio en vivo |

---

## FLUJO TÍPICO: Modificar Botón

```
┌─────────────────────────────────────────────────┐
│ 1. Editas: frontend/css/styles.css              │
│    Cambias: .btn-cocina { background: blue; }  │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│ 2. Verificas localmente: npm run dev            │
│    ✓ Ves el botón azul en localhost:3000        │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│ 3. Haces commit:                                │
│    git add .                                    │
│    git commit -m "style: botón azul"           │
│    git push origin feature/botones-azules       │
└────────────────┬────────────────────────────────┘
                 │
                 ▼ (En GitHub)
┌─────────────────────────────────────────────────┐
│ 4. Creas Pull Request                           │
│    Describes cambios                            │
│    Revisor aprueba                              │
│    "Merge pull request"                         │
└────────────────┬────────────────────────────────┘
                 │ (Automático)
                 ▼
┌─────────────────────────────────────────────────┐
│ 5. Cloudflare Pages ejecuta:                    │
│    ✓ Detecta cambios en main                    │
│    ✓ npm run build                              │
│    ✓ Deploy a CDN                               │
│    ✓ En vivo en 2-3 minutos                     │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│ 6. El mundo ve el cambio:                       │
│    https://recetas-app.pages.dev                │
│                                                 │
│    ✓ Botones ahora son azules                   │
│    ✓ Sincronizado con tu código                 │
│    ✓ Sin despliegue manual                      │
└─────────────────────────────────────────────────┘
```

---

## DURACIÓN DE CADA FASE

| Fase | Duración | Acción |
|------|----------|---------|
| Setup Git | 5 min | `git init` + GitHub |
| Setup Docker | 10 min | Instalar + Build |
| Docker Compose | 2 min | `terraform up` |
| Cloudflare Setup | 15 min | Conexión + secretos |
| **Total Setup** | **~30 min** | ✅ Todo listo |
| Deploy por cambio | 1-2 min | Automático |

---

## DIAGRAMA: Qué Sucede en Cada Lugar

```
TU PC                  GITHUB              CLOUDFLARE
│                       │                      │
├─ npm start           │                      │
│  (desarrollo local)   │                      │
│                       │                      │
├─ npm run build        │                      │
│  (preparar dist)      │                      │
│                       │                      │
├─ git push ────────────┤ main branch         │
│                       │                      │
│                       ├─ Webhook ────────────┤
│                       │                      │
│                       ├─ GitHub Actions      │
│                       │                      │
│                       ├─ Run build ─────────┤
│                       │                      │
│                       ├─ Deploy ────────────┤ Deploy a CDN
│                       │                      │
│                       │              Build artifacts
│                       │                      │
│                       │                      ├─ Replica EE.UU.
│                       │                      ├─ Replica Europa
│                       │                      ├─ Replica Asia
│                       │                      └─ Replica Otros
│                       │
                        En vivo 🌍 
                  www.example.pages.dev
```

---

## CHECKLISTA VISUAL

```
✅ FASE 1: GIT
   ├─ Cuenta GitHub creada ✓
   ├─ Repositorio creado ✓
   ├─ git init ✓
   ├─ git push ✓
   └─ Archivos en GitHub ✓

✅ FASE 2: DOCKER
   ├─ Docker instalado ✓
   ├─ Dockerfile ✓
   ├─ docker build ✓
   ├─ docker run ✓
   └─ localhost:3000 ✓

✅ FASE 3: DOCKER COMPOSE
   ├─ docker-compose.yml ✓
   ├─ docker-compose up ✓
   ├─ Backend 3000 ✓
   └─ Frontend 3001 ✓

✅ FASE 4: CLOUDFLARE
   ├─ Cuenta creada ✓
   ├─ Token generado ✓
   ├─ GitHub conectado ✓
   ├─ Secretos agregados ✓
   ├─ Build funcionando ✓
   └─ pages.dev activo ✓

🎉 LISTO PARA PRODUCCIÓN
```

---

Recuerda: Cualquier cambio que hagas y empujes a `main`, 
aparecerá automáticamente en producción en 1-3 minutos. ¡Magia! ✨
